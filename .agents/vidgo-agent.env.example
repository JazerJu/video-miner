# Agent-side VidGo API client configuration.
# Copy to ~/.vidgo-agent.env or ~/.config/vidgo/agent.env on the machine that runs the agent.
# Do not commit a real token.

VIDGO_API_BASE=https://your-vidgo-domain.com
VIDGO_API_TOKEN=<your-token>
