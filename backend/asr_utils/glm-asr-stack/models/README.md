# Models

Models are not bundled in release tarballs.

Expected runtime layout:

- `models/glm-asr-bf16/` -> `zai-org/GLM-ASR-Nano-2512`
- `models/qwen3-forcealigner-0.6b/` -> `Qwen/Qwen3-ForcedAligner-0.6B`

For local development these may be symlinks to the Hugging Face cache.
